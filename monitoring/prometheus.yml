global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'bank-churn-api'
    metrics_path: /metrics
    static_configs:
      - targets: ['host.docker.internal:5001']

#scrape_configs:
  #- job_name: 'flask-app'
  #  static_configs:
  #    - targets: ['host.docker.internal:5001']      